因为vant使用的less语言，所以先引入less的安装包
npm install less less-loader@5.0.0
npm install vant

在plugins文件夹下新建vant-ui.js
import Vue from 'vue';
import Vant from 'vant';
import 'vant/lib/index.less'
Vue.use(Vant)

nuxt.config.js中配置
plugins:[{src:'@/plugins/vant-ui.js',ssr:true}]

修改主题颜色
在nuxt.config.js中配置
build: {
		extend(config, ctx) {
	      config.module.rules.push({
	        test: /\.less$/,
	        use: [
	          {
	            loader: 'less-loader',
	            options: {
	              modifyVars: {
	                '@red': '#ff5722'
	              }
	            }
	          }
	        ]
	      })
	    }
}
