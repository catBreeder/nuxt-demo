1.在config文件夹下创建config.vue
<script>
export default{
    //在这里写自定义的变量
}
</script>

2.plugins文件夹下创建config.js
import Vue from 'vue' // vue 文件引入 - 方便在vue方法内容直接 this 调取
import Config from '../config/config.vue' // 自定变量内容 其他自便

let main = {
  install(Vue) {
    Vue.prototype.$globe = Config; // 变量的内容 后期可以在vue中 this->$globe.xxx 使用
  }
};

Vue.use(main); // 这里不能丢

// 这里是 为了在 asyncData 方法中使用
export default ({ app }, inject) => {
  // Set the function directly on the context.app object
  app.$config = Config // 名称
}
3. nuxt.config.js中引入
plugins:[
  {src: '@/plugins/main', ssr: true},
]
